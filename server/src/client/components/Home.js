import React from "react";

const Home = () => {
  return (
    <div>
      <div>Home page test</div>
      <button onClick={() => console.log("clicked")}>click</button>
    </div>
  );
};

export default Home;
